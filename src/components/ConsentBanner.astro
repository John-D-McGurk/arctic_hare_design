<div id="consent-banner">
  <h2>We value your privacy</h2>
  <p>
    We use cookies to make your experience on our site even better. Some of
    these cookies are essential, but we also use analytics cookies to understand
    how you use our site and to improve it for you.
  </p><p>
    By clicking "Allow all" you agree to our use of these cookies. If you would
    prefer to use only essential cookies, click "Allow essential".
    <p>Thanks for visiting!</p>
  </p>
  <div class="justify-end">
  <button id="no-consent" class="btn btn-secondary">Allow essential</button>
  <button id="consent" class="btn btn-primary">Allow all</button></div>
</div>

<style>
    #consent-banner {
        font-family: Verdana, sans-serif;
        position: fixed;
        bottom: 0;
        left: 0;
        width: 100%;
        z-index: 1000;
        background: var(--black);
        color: var(--white);
        box-sizing: border-box;
        padding: var(--space-md) var(--space-lg);
        border-top: 2px solid;
        transform: translateY(100%);
        transition: transform 0.8s;
    }

    #consent-banner.show {
        transform: translateY(0);
    }

    #consent-banner h2 {
        font-family: Verdana, sans-serif;
        font-weight: 600;
        font-size: var(--text-md);
    }

    #consent-banner p {
        font-size: var(--text-sm);
    }
    
    .btn {
        border-radius: 0;
    }

    .btn-secondary {
        color: var(--white);
    }

    .justify-end {
        gap: var(--space-sm);
    }

    @media only screen and (max-width: 768px) {
    #consent-banner {
        padding: var(--space-sm) var(--space-xs);
    }

    .justify-end {
        justify-content: center;
        padding: var(--space-sm) 0;
    }
    }
</style>